---
phase: 05-embedding-pipeline
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/data/embeddings.json
  - package.json
autonomous: false
requirements:
  - EMBED-04

must_haves:
  truths:
    - "src/data/embeddings.json contains one entry per document with real UMAP x/y coordinates (not the placeholder zeros)"
    - "Running node scripts/embed.mjs a second time with no changes prints all cache hits and does not modify embeddings.json"
    - "npm run build succeeds using the committed embeddings.json"
    - "Running node scripts/embed.mjs with Ollama stopped prints an explicit actionable error and exits non-zero"
  artifacts:
    - path: "src/data/embeddings.json"
      provides: "Committed artifact with real 2D coordinates for all documents"
      contains: '"x"'
    - path: "package.json"
      provides: "embed script registered as npm script for convenience"
  key_links:
    - from: "src/data/embeddings.json"
      to: "Astro build"
      via: "Astro can import JSON from src/data/ at compile time"
      pattern: '"documents"'
    - from: "scripts/embed.mjs"
      to: "src/data/embeddings.json"
      via: "atomicWriteJSON — .tmp + renameSync"
      pattern: "OUTPUT_PATH"
---

<objective>
Run `scripts/embed.mjs` for real against a live Ollama instance to produce the committed `src/data/embeddings.json` artifact, verify the incremental cache fast path and the Ollama failure path, confirm `npm run build` succeeds, and add the `embed` npm script for convenience.

Purpose: EMBED-04 requires embeddings.json to be committed to git and consumed at Astro build time. This plan closes the loop — implementation (Plan 02) is inert until this plan executes it and commits the real output.

Output: A committed `src/data/embeddings.json` with real UMAP coordinates for all 15 documents; `npm run build` passes.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-embedding-pipeline/05-RESEARCH.md
@.planning/phases/05-embedding-pipeline/05-VALIDATION.md
@.planning/phases/05-embedding-pipeline/05-01-SUMMARY.md
@.planning/phases/05-embedding-pipeline/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add embed npm script and verify EMBED-03 failure path</name>
  <files>package.json</files>
  <action>
Add `"embed": "node scripts/embed.mjs"` to the `"scripts"` section in `package.json` so the operator can run `npm run embed` without remembering the full path.

Then verify EMBED-03 (Ollama failure path) manually:
- If Ollama is currently running, do NOT stop it — proceed to the next test. Instead, test the failure path by temporarily pointing to a wrong port: `node -e "import('./scripts/embed.mjs').then(m => m.assertOllamaRunning('http://localhost:9999'))"` — this should print the ECONNREFUSED error message and exit 1. This validates EMBED-03 without disrupting Ollama.
- Confirm the error message contains actionable text ("Ollama is not running" or similar).
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.assert(p.scripts.embed, 'embed script missing'); console.log('embed script ok');"</automated>
  </verify>
  <done>`npm run embed` is a valid command; EMBED-03 failure path confirmed to print actionable error and exit 1</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Run embed.mjs against live Ollama and verify all EMBED behaviors</name>
  <files>src/data/embeddings.json</files>
  <action>
Operator runs `npm run embed` against live Ollama (with nomic-embed-text pulled) to produce the real embeddings artifact. Claude cannot call Ollama — this requires the operator to run the pipeline and confirm the output.
  </action>
  <what-built>
    scripts/embed.mjs is fully implemented. Now we need to run it against live Ollama to produce the real embeddings.json artifact.

    Before this checkpoint, Claude has:
    1. Added "embed" to package.json scripts
    2. Verified the EMBED-03 failure path (actionable error on connection refused)
    3. All automated tests pass: `node scripts/embed.test.mjs`
  </what-built>
  <how-to-verify>
    **Pre-requisite:** Ollama must be running with nomic-embed-text available.

    Step 1 — Confirm Ollama is running:
    ```
    curl http://localhost:11434
    ```
    Expected: "Ollama is running"

    Step 2 — If nomic-embed-text is not yet pulled:
    ```
    ollama pull nomic-embed-text
    ```

    Step 3 — Run the embedding pipeline:
    ```
    npm run embed
    ```
    Expected output:
    - "Found N documents" (should be ~15)
    - "[embedding] ai-tools-and-services/langchain.md" (one line per doc, first run)
    - "UMAP: 15 docs, nNeighbors=14"
    - "Done. Wrote 15 document coordinates to src/data/embeddings.json"

    Step 4 — Verify the output file:
    ```
    node -e "const d = JSON.parse(require('fs').readFileSync('src/data/embeddings.json','utf8')); console.log('docs:', d.documents.length); console.log('sample:', JSON.stringify(d.documents[0]));"
    ```
    Expected: documents count > 0, each entry has id, title, category, tags, x, y with non-zero x/y values

    Step 5 — Verify incremental cache (EMBED-02):
    ```
    npm run embed
    ```
    Expected: All lines show "[cache hit]", ends with "All documents cached — skipping UMAP and output write"

    Step 6 — Verify build still passes (EMBED-04):
    ```
    npm run build
    ```
    Expected: Build completes without errors mentioning embeddings.json

    Step 7 — Run full test suite:
    ```
    node src/lib/graph.test.mjs && node scripts/embed.test.mjs
    ```
    Expected: All tests pass, exits 0
  </how-to-verify>
  <verify>
    <automated>node -e "const d = JSON.parse(require('fs').readFileSync('src/data/embeddings.json','utf8')); console.assert(d.documents.length > 0, 'no docs'); console.assert(typeof d.documents[0].x === 'number', 'missing x'); console.log('embeddings.json has', d.documents.length, 'docs with real coordinates');"</automated>
  </verify>
  <done>embeddings.json has real x/y coordinates; incremental cache fast path confirmed; npm run build passes</done>
  <resume-signal>Type "approved" once all steps pass, or describe any issues encountered</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit the real embeddings.json artifact</name>
  <files>src/data/embeddings.json</files>
  <action>
After the human verify checkpoint confirms embeddings.json has real coordinates:

1. Stage and commit `src/data/embeddings.json`:
   ```
   git add src/data/embeddings.json package.json
   git commit -m "feat(05): add embedding pipeline and commit initial embeddings.json"
   ```

2. Confirm the committed file has real x/y data (not the zero-placeholder):
   ```
   git show HEAD:src/data/embeddings.json | node -e "const d = JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.assert(d.documents.length > 0, 'empty'); console.assert(d.documents[0].x !== 0 || d.documents[0].y !== 0, 'placeholder zeros'); console.log('committed artifact ok');"
   ```

3. Run the full test suite one final time to confirm green state:
   ```
   node src/lib/graph.test.mjs && node scripts/embed.test.mjs
   ```

Note on UMAP non-determinism (documented in research): Each run produces slightly different x/y coordinates because umap-js uses random initialization without an exposed seed API. This is expected and documented. The incremental cache means UMAP only re-runs when documents actually change, so git noise is minimal in practice.
  </action>
  <verify>
    <automated>node src/lib/graph.test.mjs && node scripts/embed.test.mjs</automated>
  </verify>
  <done>
- `src/data/embeddings.json` is committed to git with real UMAP coordinates (non-zero x/y, count > 0)
- `npm run build` succeeds
- Full test suite exits 0
- `.cache/embed-vectors.json` is NOT tracked by git (verify: `git status .cache/` shows untracked or clean)
  </done>
</task>

</tasks>

<verification>
Phase 5 complete when all four success criteria from ROADMAP.md are true:
1. `npm run embed` produces `src/data/embeddings.json` with x/y for every document
2. Second run with no doc changes shows all cache hits and skips UMAP
3. Running with Ollama stopped prints explicit error and exits non-zero — no output file written
4. `npm run build` succeeds using committed embeddings.json
</verification>

<success_criteria>
- `git log --oneline -1` shows commit containing embeddings.json
- `node -e "const d = JSON.parse(require('fs').readFileSync('src/data/embeddings.json','utf8')); console.assert(d.documents.length > 0); console.log(d.documents.length, 'docs')"` prints count > 0
- `npm run build` exits 0
- `node src/lib/graph.test.mjs && node scripts/embed.test.mjs` exits 0
- `git status .cache/` shows `.cache/` is not tracked (gitignored)
</success_criteria>

<output>
After completion, create `.planning/phases/05-embedding-pipeline/05-03-SUMMARY.md`
</output>
