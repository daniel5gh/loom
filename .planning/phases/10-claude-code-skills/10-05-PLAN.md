---
phase: 10-claude-code-skills
plan: 05
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
  - 10-04
files_modified: []
autonomous: false
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-05
must_haves:
  truths:
    - "/loom:add creates a valid, non-committed document when given a real URL"
    - "/loom:deploy runs the full pipeline and pushes without errors"
    - "/loom:deploy stops cleanly when given a doc with bad frontmatter"
    - "/loom:retag correctly updates tags across documents"
    - "/loom:gaps produces an actionable gap report with suggested topics"
  artifacts:
    - path: ".claude/skills/add/SKILL.md"
      provides: "/loom:add ready for use"
    - path: ".claude/skills/deploy/SKILL.md"
      provides: "/loom:deploy ready for use"
    - path: ".claude/skills/retag/SKILL.md"
      provides: "/loom:retag ready for use"
    - path: ".claude/skills/gaps/SKILL.md"
      provides: "/loom:gaps ready for use"
    - path: "scripts/validate-docs.mjs"
      provides: "Validation script used by /loom:deploy"
  key_links:
    - from: "/loom:deploy"
      to: "scripts/validate-docs.mjs"
      via: "node scripts/validate-docs.mjs Bash call"
      pattern: "validate-docs"
    - from: "/loom:deploy"
      to: "scripts/embed.mjs"
      via: "node scripts/embed.mjs Bash call"
      pattern: "embed.mjs"
---

<objective>
Human verification of all four new Claude Code skills against the live Loom system.

Purpose: Skills are prose instructions executed by Claude using live tools. Their correctness can only be verified by running them in a real Claude Code session. This checkpoint confirms all five SKILL requirements are met before the phase is closed.
Output: Human sign-off that all four skills work as specified.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated test suite before checkpoint</name>
  <files>scripts/validate-docs.test.mjs</files>
  <action>
    Run the full automated test suite to confirm the validation script is correct before the human verification session:

    node scripts/validate-docs.test.mjs
    node scripts/embed.test.mjs

    Both must exit 0 (green). If either fails, stop and fix the failure before proceeding to the human checkpoint.

    Also confirm all four skill files exist:
    - ls .claude/skills/add/SKILL.md
    - ls .claude/skills/deploy/SKILL.md
    - ls .claude/skills/retag/SKILL.md
    - ls .claude/skills/gaps/SKILL.md

    Report the results.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom &amp;&amp; node scripts/validate-docs.test.mjs &amp;&amp; node scripts/embed.test.mjs &amp;&amp; ls .claude/skills/add/SKILL.md .claude/skills/deploy/SKILL.md .claude/skills/retag/SKILL.md .claude/skills/gaps/SKILL.md</automated>
  </verify>
  <done>Both test files exit 0. All four SKILL.md files exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all four skills</name>
  <action>
    Pause for human verification. The executor should present the verification instructions below and wait for the human to run each skill test in a fresh Claude Code session. Do not proceed until the human types "approved".
  </action>
  <verify>
    <automated>MISSING — this task requires human verification in a live Claude Code session</automated>
  </verify>
  <done>Human types "approved" after confirming all five skill tests pass.</done>
  <what-built>
    Four new Claude Code slash commands:
    - /loom:add -- fetch URL, create document with vocabulary-sourced tags (SKILL-01)
    - /loom:deploy -- validate + embed + commit + push pipeline (SKILL-02, SKILL-03)
    - /loom:retag -- merge or split tags across corpus (SKILL-04)
    - /loom:gaps -- thin-coverage tag analysis with topic suggestions (SKILL-05)
    Plus: scripts/validate-docs.mjs standalone validator used by /loom:deploy
  </what-built>
  <how-to-verify>
    Open a NEW Claude Code session (fresh context) in the loom project directory and run each skill:

    **Test 1 -- /loom:add (SKILL-01):**
    Run: /loom:add https://ollama.com/blog/tool-support
    Expected: A new .md file created in ai-tools-and-services/ (or appropriate dir), with correct frontmatter (title, date, tags from existing vocabulary), Summary and Content sections, References section. No git commit made. Tags are all normalized (lowercase, hyphenated).

    **Test 2 -- /loom:deploy validation gate (SKILL-03):**
    Temporarily corrupt a document's frontmatter (e.g., remove the date field from any .md file).
    Run: /loom:deploy
    Expected: Skill prints validation errors listing the corrupted file and stops. No embed.mjs is run. No git commit is made.
    Restore the corrupted file after confirming the stop behavior.

    **Test 3 -- /loom:deploy full pipeline (SKILL-02):**
    After restoring the document, run: /loom:deploy
    Expected: Validation passes, embed.mjs runs (or reports cache hit), embeddings.json staged, committed (if changed), pushed. Cloudflare Pages build should be triggered (check the Pages dashboard or wait for build notification).

    **Test 4 -- /loom:retag (SKILL-04):**
    Pick any tag that appears in 2+ documents (use /loom:gaps output for a suggestion).
    Run: /loom:retag existing-tag existing-tag-v2
    Expected: All documents with that tag now have the new tag. Old tag gone. No other content changed. Report shows affected file list.
    (Optional: revert the change after confirming.)

    **Test 5 -- /loom:gaps (SKILL-05):**
    Run: /loom:gaps
    Expected: A structured report listing tags with 1-2 documents, the document titles using each thin tag, and 2-3 specific suggested research topics per thin tag. Summary line with total document count.
  </how-to-verify>
  <resume-signal>Type "approved" if all five tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All five SKILL requirements verified by human in live Claude Code session:
- SKILL-01: /loom:add creates valid non-committed document with vocabulary tags
- SKILL-02: /loom:deploy runs full pipeline and pushes
- SKILL-03: /loom:deploy stops cleanly on validation failure
- SKILL-04: /loom:retag updates tags across corpus
- SKILL-05: /loom:gaps produces structured gap report
</verification>

<success_criteria>
Human confirms all four skills work correctly in a live Claude Code session. Phase 10 complete. v1.1 Visualization milestone shipped.
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-code-skills/10-05-SUMMARY.md`
</output>
