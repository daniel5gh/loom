---
phase: 10-claude-code-skills
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - .claude/skills/deploy/SKILL.md
autonomous: true
requirements:
  - SKILL-02
  - SKILL-03
must_haves:
  truths:
    - "Running /loom:deploy validates all documents, runs embed.mjs, commits embeddings.json, and pushes"
    - "If any document fails validation, /loom:deploy prints errors and stops — no embed, no commit, no push"
    - "If embeddings.json is unchanged after embed.mjs runs, no commit is made (no false failure)"
    - "Other staged files are NOT swept into the embeddings commit"
  artifacts:
    - path: ".claude/skills/deploy/SKILL.md"
      provides: "/loom:deploy skill instructions"
      contains: "loom:deploy"
  key_links:
    - from: ".claude/skills/deploy/SKILL.md"
      to: "scripts/validate-docs.mjs"
      via: "node scripts/validate-docs.mjs via Bash"
      pattern: "validate-docs"
    - from: ".claude/skills/deploy/SKILL.md"
      to: "scripts/embed.mjs"
      via: "node scripts/embed.mjs via Bash"
      pattern: "embed.mjs"
    - from: ".claude/skills/deploy/SKILL.md"
      to: "git push"
      via: "git add embeddings.json; git diff --cached; git commit; git push"
      pattern: "git push"
---

<objective>
Create `.claude/skills/deploy/SKILL.md` — the `/loom:deploy` Claude Code slash command that orchestrates the full deploy pipeline: validate all documents, re-embed changed docs via Ollama, commit updated embeddings.json, and push to trigger a Cloudflare Pages build.

Purpose: One command replaces the multi-step manual deploy workflow. The hard stop on validation errors (SKILL-03) prevents bad documents from reaching production.
Output: .claude/skills/deploy/SKILL.md
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/skills/research/SKILL.md
@scripts/embed.mjs
</context>

<interfaces>
<!-- Critical existing scripts the deploy skill orchestrates. -->

scripts/validate-docs.mjs (created in Plan 01):
- Run: node scripts/validate-docs.mjs
- Exit 0: all documents valid, prints "All N documents valid."
- Exit 1: prints validation errors, one per line

scripts/embed.mjs:
- Run: node scripts/embed.mjs
- Handles incremental caching (only re-embeds changed docs)
- assertOllamaRunning() exits non-zero with clear message if Ollama at http://10.0.1.3:11434 is unreachable
- On success: updates src/data/embeddings.json (or no-ops if all cache hits)

Git sequence for deploy:
```bash
# Stage only embeddings.json (not other staged files)
git add src/data/embeddings.json

# Check if anything actually changed (exit 0 = no changes, exit 1 = changes staged)
git diff --cached --quiet

# Only commit if exit code was 1 (changes exist)
git commit -m "chore: update embeddings"
git push
```

Git permission: Bash tool has git:* permission (settings.local.json confirmed).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write /loom:deploy SKILL.md</name>
  <files>.claude/skills/deploy/SKILL.md</files>
  <action>
    Create the directory `.claude/skills/deploy/` and write `SKILL.md`.

    YAML frontmatter block at top of file:
    - name: loom:deploy
    - description: Validate documents, re-embed changed docs, commit and push to trigger Cloudflare Pages build
    - disable-model-invocation: true
    - allowed-tools: Read, Glob, Bash

    Body -- numbered steps for the skill to follow:

    Step 1 (VALIDATION -- hard stop if errors):
    Run validation via Bash: `node scripts/validate-docs.mjs`
    - If exit code is 0: print "Validation passed. Proceeding to embed step." and continue.
    - If exit code is non-zero: print the validation output (already printed by the script), then print "Deploy aborted: fix validation errors before deploying." and STOP. Do not proceed to any further steps.

    Step 2 (EMBED):
    Run embedding pipeline via Bash: `node scripts/embed.mjs`
    - If exit code is non-zero (Ollama unreachable, or other error): print the error output and STOP. Print "Deploy aborted: embedding pipeline failed."
    - If exit code is 0: continue.

    Step 3 (CHECK FOR OTHER STAGED FILES -- safety):
    Run via Bash: `git status --porcelain`
    - If any lines do NOT start with " " (i.e., there are already-staged files other than embeddings.json): warn the user: "Warning: you have other staged files. Only src/data/embeddings.json will be committed by this deploy. Your other staged changes remain staged but are NOT included in this commit."
    - Continue regardless (warning only, not a stop).

    Step 4 (STAGE EMBEDDINGS):
    Run via Bash: `git add src/data/embeddings.json`

    Step 5 (CHECK IF EMBEDDINGS CHANGED):
    Run via Bash: `git diff --cached --quiet`
    - Exit code 0 means no changes staged (embeddings.json was identical). Print "Embeddings up to date — nothing to commit. Pushing current HEAD." and skip to Step 6 (push only).
    - Exit code 1 means changes staged. Proceed to commit.

    Step 5a (COMMIT -- only if changes):
    Run via Bash: `git commit -m "chore: update embeddings"`
    - If non-zero exit: print error and STOP.

    Step 6 (PUSH):
    Run via Bash: `git push`
    - If non-zero exit: print git error output and print "Deploy failed at push step. Check git remote and authentication."
    - If exit 0: print "Deploy complete. Cloudflare Pages build triggered."

    Critical guardrails (must appear verbatim in the skill body):
    - "STOP if validation fails. Do not proceed to embed or commit."
    - "Only git add src/data/embeddings.json -- do not stage other files."
    - "Check git diff --cached --quiet before committing to avoid 'nothing to commit' error."
  </action>
  <verify>
    <automated>test -f /home/daniel/codes/loom/.claude/skills/deploy/SKILL.md &amp;&amp; grep -q "validate-docs.mjs" /home/daniel/codes/loom/.claude/skills/deploy/SKILL.md &amp;&amp; grep -q "embed.mjs" /home/daniel/codes/loom/.claude/skills/deploy/SKILL.md &amp;&amp; grep -q "git diff --cached" /home/daniel/codes/loom/.claude/skills/deploy/SKILL.md &amp;&amp; echo "deploy SKILL.md OK"</automated>
  </verify>
  <done>
    .claude/skills/deploy/SKILL.md exists. Skill body references: validate-docs.mjs (with hard-stop on non-zero exit), embed.mjs, git diff --cached --quiet guard, git add src/data/embeddings.json only, git push. YAML frontmatter has name: loom:deploy and disable-model-invocation: true.
  </done>
</task>

</tasks>

<verification>
- File exists: `.claude/skills/deploy/SKILL.md`
- Grep confirms: validate-docs.mjs, embed.mjs, git diff --cached, git push all referenced
- Hard-stop language for validation failure is present
- "Only git add src/data/embeddings.json" guard is present
</verification>

<success_criteria>
/loom:deploy skill correctly orchestrates the full pipeline. Validation failures cause an immediate stop with clear error output. Unchanged embeddings skip the commit step cleanly. Only embeddings.json is included in the commit.
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-code-skills/10-03-SUMMARY.md`
</output>
