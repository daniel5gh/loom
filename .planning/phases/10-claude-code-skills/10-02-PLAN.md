---
phase: 10-claude-code-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/add/SKILL.md
autonomous: true
requirements:
  - SKILL-01
must_haves:
  truths:
    - "Running /loom:add <url> creates a markdown file in the correct category directory"
    - "The created file has title, date, tags (from existing vocabulary only), and Summary + Content sections"
    - "The file is NOT committed — only written to disk"
    - "Tags are drawn from existing vocabulary (not invented) and pass normalization"
  artifacts:
    - path: ".claude/skills/add/SKILL.md"
      provides: "/loom:add skill instructions"
      contains: "loom:add"
  key_links:
    - from: ".claude/skills/add/SKILL.md"
      to: "ai-tools-and-services/, cloud-ai-platforms/, companies/"
      via: "category decision rules in skill instructions"
      pattern: "category"
    - from: ".claude/skills/add/SKILL.md"
      to: "existing tag vocabulary"
      via: "Glob all docs, Read frontmatter, build vocabulary list"
      pattern: "vocabulary"
---

<objective>
Create `.claude/skills/add/SKILL.md` — the `/loom:add <url>` Claude Code slash command that fetches a URL, summarizes content, infers tags from the existing vocabulary, and writes a properly formatted document.

Purpose: Lets the operator capture external content into the knowledge base with a single command, without leaving the Claude Code session.
Output: .claude/skills/add/SKILL.md
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/skills/research/SKILL.md
@.claude/skills/research/template.md
</context>

<interfaces>
<!-- Key patterns from existing skills the executor MUST follow. -->

From .claude/skills/research/SKILL.md (established skill format):
```yaml
---
name: loom:research
description: Research a topic and create a new Loom knowledge base document
argument-hint: "<topic>"
disable-model-invocation: true
allowed-tools: Read, Write, WebSearch, Bash, Glob
---
```

Tag normalization rule (enforced across all skills):
```
tag.toLowerCase().trim().replace(/\s+/g, '-')
"Machine Learning" -> "machine-learning"
"LLM" -> "llm"
```

Category rules (from research skill, same rules apply):
```
ai-tools-and-services/ -- tools, platforms, frameworks, libraries
cloud-ai-platforms/    -- cloud provider AI services (AWS, GCP, Azure, etc.)
companies/             -- companies as subjects (not their tools)
```

Document structure (from template.md):
```markdown
---
title: string
date: YYYY-MM-DD
tags: [tag1, tag2]
---
# title

## Summary
2-4 sentences.

## Content
Substantive content with subsections.

## References
- URL
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write /loom:add SKILL.md</name>
  <files>.claude/skills/add/SKILL.md</files>
  <action>
    Create the directory `.claude/skills/add/` and write `SKILL.md`.

    YAML frontmatter block at top of file:
    - name: loom:add
    - description: Fetch a URL and create a new Loom knowledge base document
    - argument-hint: "&lt;url&gt;"
    - disable-model-invocation: true
    - allowed-tools: Read, Write, Glob, WebFetch, WebSearch, Bash

    Body — numbered steps for the skill to follow:

    Step 1: Fetch the URL ($ARGUMENTS) using WebFetch. Extract: page title, main content, key concepts. If WebFetch fails (paywall/auth error), fall back to WebSearch with the URL as query to get a summary.

    Step 2: Build the existing tag vocabulary. Glob all *.md files in ai-tools-and-services/, cloud-ai-platforms/, companies/. Read each file. Collect all unique tags from frontmatter into a vocabulary list.

    Step 3: Infer 3-6 tags from the vocabulary list that best match the fetched content. ONLY use tags that exist in the vocabulary. Do NOT invent new tags. If no existing tag fits precisely, use the closest match. Apply normalization to every tag: tag.toLowerCase().trim().replace(/\s+/g, '-')

    Step 4: Choose category directory using these exact rules (no ambiguity):
    - cloud-ai-platforms/ -- content primarily about a cloud provider AI service (AWS Bedrock, GCP Vertex, Azure AI, Google Cloud AI, Microsoft Azure AI)
    - companies/ -- content about a company as a subject (company profile, funding round, acquisition news)
    - ai-tools-and-services/ -- everything else (tools, frameworks, libraries, models, APIs, platforms)

    Step 5: Create the filename -- lowercase, hyphen-separated slug derived from the page title. Example: "Anthropic Claude 3.5" becomes "anthropic-claude-35.md"

    Step 6: Write the document to &lt;category&gt;/&lt;slug&gt;.md with this exact structure:
    - YAML frontmatter: title (string, must match H1 exactly), date (today as YYYY-MM-DD), tags (array of normalized strings)
    - H1 heading matching title field exactly
    - ## Summary section: 2-4 sentences describing what it is and why it matters for AI/ML practitioners
    - ## Content section: substantive content with subsections as needed
    - ## References section containing the source URL

    Step 7: DO NOT run git add, git commit, or any other git command. The file is created on disk only.

    Step 8: Display the created file path, the YAML frontmatter block, and the message: "File created (not committed). Run /loom:deploy when ready to publish."

    Critical guardrails (must appear verbatim in the skill body):
    - "ONLY use tags from the existing vocabulary. Do NOT invent new tags."
    - "Do NOT run any git commands. No git add, no git commit."
    - "Tag normalization is REQUIRED: tag.toLowerCase().trim().replace(/\s+/g, '-')"
  </action>
  <verify>
    <automated>test -f /home/daniel/codes/loom/.claude/skills/add/SKILL.md &amp;&amp; grep -q "loom:add" /home/daniel/codes/loom/.claude/skills/add/SKILL.md &amp;&amp; grep -q "WebFetch" /home/daniel/codes/loom/.claude/skills/add/SKILL.md &amp;&amp; grep -q "vocabulary" /home/daniel/codes/loom/.claude/skills/add/SKILL.md &amp;&amp; echo "SKILL.md OK"</automated>
  </verify>
  <done>
    .claude/skills/add/SKILL.md exists with correct YAML frontmatter (name: loom:add, disable-model-invocation: true, WebFetch in allowed-tools). Skill body includes: vocabulary-building step, category decision rules, no-git-commit guardrail, tag normalization rule.
  </done>
</task>

</tasks>

<verification>
- File exists: `.claude/skills/add/SKILL.md`
- Frontmatter has `name: loom:add` and `disable-model-invocation: true`
- `WebFetch` and `WebSearch` both appear in `allowed-tools`
- Skill body mentions "vocabulary" (tag vocabulary building step)
- Skill body contains explicit "DO NOT" git guardrail
</verification>

<success_criteria>
/loom:add skill is ready to use. Running `/loom:add https://example.com/article` in a Claude Code session will create a formatted markdown document in the correct content directory with tags sourced from the existing vocabulary.
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-code-skills/10-02-SUMMARY.md`
</output>
