---
phase: 09-home-page
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified: []
autonomous: false
requirements:
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "User can see the loom> _ terminal prompt on the home page"
    - "Clicking the prompt opens the global shell overlay"
    - "Pressing / on the home page opens the global shell overlay"
    - "The home page lists 10 recent articles in reverse-chronological order with title, date, and tags"
    - "Vim j/k navigation works on the home page recent list"
  artifacts:
    - path: src/pages/index.astro
      provides: "Verified working home page"
    - path: src/layouts/Base.astro
      provides: "Verified loom:open-search event handler"
  key_links:
    - from: ".home-prompt click"
      to: "shell-overlay"
      via: "loom:open-search CustomEvent"
      pattern: "overlay visible after click"
---

<objective>
Human visual verification of the rebuilt home page — confirm terminal prompt, search overlay integration, recent articles list, and vim navigation all work correctly in the browser.

Purpose: HOME-01 (prompt visible + click opens overlay) and HOME-02 (10 recent articles displayed) require live browser verification. No automated test can confirm DOM rendering, click behavior, or visual correctness.

Output: All Phase 9 behaviors confirmed or issues filed for gap closure.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-home-page/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run full test suite</name>
  <files></files>
  <action>
    Run the full automated test suite, then start the Astro dev server for human verification:

    1. `node --test test/` — all tests must be green before proceeding
    2. `npm run dev` — start dev server at http://localhost:4321

    If any tests fail, stop and report failures. Do not proceed to the checkpoint if automated tests are red.
  </action>
  <verify>
    <automated>node --test test/</automated>
  </verify>
  <done>Full test suite passes. Dev server running and accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — home page behaviors</name>
  <files></files>
  <action>
    Present the verification checklist to the human. All 6 checks must be confirmed before Phase 9 is marked complete.
  </action>
  <what-built>
    Rebuilt home page (src/pages/index.astro) with:
    - Terminal prompt (`loom> _`) as primary call-to-action
    - loom:open-search CustomEvent channel from prompt click to Base.astro openOverlay
    - 10 most recently added articles in reverse-chronological order via DocCard
    - Vim j/k navigation on the recent list
  </what-built>
  <how-to-verify>
    Visit http://localhost:4321 and verify:

    1. **Terminal prompt visible:** The page shows `loom> _` with a blinking underscore cursor above the recent list. The prompt has a cyan border and glows on hover.

    2. **Click opens overlay:** Click the `loom> _` prompt. The shell overlay should open (`loom>` input field, blinking cursor, empty results). Press Esc to close.

    3. **/ keypress opens overlay:** Click anywhere on the page body (not the prompt), then press `/`. The shell overlay should open. Press Esc to close.

    4. **Recent articles list:** Below the AsciiDivider, a `// recent` heading appears, followed by up to 10 article cards. Each card shows title, date, and tags. Verify the dates are in reverse order (newest first). Verify the list is NOT grouped by category.

    5. **Vim navigation:** Press `j` — the first card should highlight. Press `j` again to move down. Press `k` to move up. Press `G` to jump to the last card. Press `gg` to jump back to the first. The status bar at the bottom should show `1/N`, `2/N`, etc.

    6. **No category grid:** Confirm there are NO "AI Tools and Services", "Cloud AI Platforms", or "Companies" section headings on the page.
  </how-to-verify>
  <verify>
    <automated>MISSING — HOME-01 visual behavior requires human browser verification</automated>
  </verify>
  <done>Human approves all 6 verification checks.</done>
  <resume-signal>Type "approved" if all 6 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 manual verification checks pass:
- Terminal prompt visible with blinking cursor
- Click and / both open shell overlay
- 10 recent articles listed newest-first
- Vim j/k/G/gg navigation works with status bar
- No category-grid sections present
</verification>

<success_criteria>
Human approves all 6 verification checks. Phase 9 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/09-home-page/09-03-SUMMARY.md`
</output>
