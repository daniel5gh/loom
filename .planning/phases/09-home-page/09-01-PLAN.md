---
phase: 09-home-page
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/home.test.mjs
autonomous: true
requirements:
  - HOME-02

must_haves:
  truths:
    - "A test file exists that verifies the sort+slice logic produces at most 10 docs in descending date order"
    - "The test draws from all three collections (aiTools, cloudPlatforms, companies) in its fixture data"
    - "The test is runnable with `node --test test/home.test.mjs` and passes with zero implementation"
  artifacts:
    - path: test/home.test.mjs
      provides: "Unit tests for HOME-02 sort+slice logic"
      exports: []
  key_links:
    - from: test/home.test.mjs
      to: "sort+slice logic (to be implemented in index.astro)"
      via: "inline pure function extracted from component frontmatter"
      pattern: "sort.*getTime|slice\\(0, 10\\)"
---

<objective>
Create the test scaffold for HOME-02 before implementing the home page rewrite.

Purpose: Establish the test contract for the sort+slice logic. The test must be RED before Wave 1 begins (the function does not exist yet) so that Wave 1 can turn it GREEN.

Output: `test/home.test.mjs` with unit tests verifying that a merged+sorted+sliced array of docs produces at most 10 entries in reverse-chronological order, drawing from all three collections.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-home-page/09-RESEARCH.md
@.planning/phases/09-home-page/09-VALIDATION.md

<interfaces>
<!-- Existing test files — follow their patterns exactly -->

From test/shell.test.mjs (pattern reference — node:test, describe/it, assert):
Uses `import { describe, it } from 'node:test'` and `import assert from 'node:assert/strict'`.
No external test dependencies.

From test/contrast.test.mjs (pattern reference):
Same pattern. Tests are self-contained pure functions.

HOME-02 logic to test: merge allDocs from three collections, sort by doc.data.date.getTime() descending, slice(0, 10).
The unit test should test this as an extracted pure function — NOT the Astro page itself.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test/home.test.mjs — sort+slice unit tests</name>
  <files>test/home.test.mjs</files>
  <behavior>
    - Test: merged array of 15 docs from 3 collections → result length is 10
    - Test: result is sorted newest-first (result[0].data.date >= result[1].data.date for all adjacent pairs)
    - Test: all three collection sources are eligible (fixture data includes docs from aiTools, cloudPlatforms, and companies; the 10 most recent may span collections)
    - Test: if total docs <= 10, result length equals total count (no over-slicing)
    - Test: docs with identical dates are included without error (stable output, no exception)
  </behavior>
  <action>
    Create `test/home.test.mjs` using `node:test` built-in. Do NOT install any test framework.

    Structure: Extract the sort+slice logic into a local pure function `getRecentDocs(allDocs, n)` that the test imports inline (the function is declared in the test file itself — it mirrors what index.astro will implement). This lets the test be self-contained without importing Astro pages.

    ```javascript
    // The pure function that mirrors the index.astro implementation
    function getRecentDocs(allDocs, n = 10) {
      return [...allDocs]
        .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
        .slice(0, n);
    }
    ```

    Build fixture data as plain objects with the shape `{ collection: string, data: { title: string, date: Date } }`. Create at least 15 fixture docs spanning all three collections with varied dates (including some with the same date).

    Do NOT use `is:inline`, `import ... from 'astro:content'`, or any Astro-specific API. This is a pure Node.js test.
  </action>
  <verify>
    <automated>node --test test/home.test.mjs</automated>
  </verify>
  <done>All tests pass. `node --test test/home.test.mjs` exits 0 with all assertions green.</done>
</task>

</tasks>

<verification>
`node --test test/home.test.mjs` passes with zero failures.
`node --test test/` passes (existing shell and contrast tests unaffected).
</verification>

<success_criteria>
`test/home.test.mjs` exists, covers all 5 behaviors listed in the task, and passes cleanly with `node --test`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-home-page/09-01-SUMMARY.md`
</output>
