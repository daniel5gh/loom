---
phase: 08-global-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/shell.test.mjs
  - test/contrast.test.mjs
autonomous: true
requirements:
  - SHELL-02
  - AESTH-04
must_haves:
  truths:
    - "Fuse.js multi-key search returns ranked results for title, tag, and summary queries"
    - "All project neon accent colors meet WCAG AA 4.5:1 contrast against --bg-base (#0D0D1A)"
  artifacts:
    - path: "test/shell.test.mjs"
      provides: "Unit tests for Fuse.js config used by search overlay"
      exports: []
    - path: "test/contrast.test.mjs"
      provides: "Offline contrast ratio verification for aesthetic colors"
      exports: []
  key_links:
    - from: "test/shell.test.mjs"
      to: "fuse.js"
      via: "npm import"
      pattern: "import Fuse from 'fuse.js'"
    - from: "test/contrast.test.mjs"
      to: "CSS custom properties"
      via: "hardcoded hex values matching :root in global.css"
      pattern: "0D0D1A.*D0D0D0|00E5FF|FF2D78|39FF14"
---

<objective>
Create test scaffolds for Phase 8's two unit-testable concerns: Fuse.js search configuration and WCAG contrast verification.

Purpose: Establish automated feedback before shell implementation begins. These tests define the contracts the shell implementation must satisfy.
Output: `test/shell.test.mjs` and `test/contrast.test.mjs` — both passing with `node --test test/`
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-global-shell/08-RESEARCH.md
@.planning/phases/08-global-shell/08-VALIDATION.md

<interfaces>
<!-- Project color palette from public/styles/global.css :root -->
:root {
  --bg-base:        #0D0D1A;
  --bg-surface:     #13132A;
  --bg-elevated:    #1A1A3A;
  --text-primary:   #D0D0D0;
  --text-secondary: #8888AA;
  --text-dim:       #555577;
  --neon-cyan:      #00E5FF;
  --neon-magenta:   #FF2D78;
  --neon-green:     #39FF14;
}

<!-- Fuse.js config to use in shell (per research) -->
{
  keys: [
    { name: 'title',   weight: 0.6 },
    { name: 'tags',    weight: 0.3 },
    { name: 'summary', weight: 0.1 },
  ],
  threshold: 0.35,
  includeScore: false,
}

<!-- WCAG AA requirement: contrast ratio >= 4.5:1 for normal text -->
<!-- Contrast ratio formula: (L1 + 0.05) / (L2 + 0.05) where L1 > L2 -->
<!-- Relative luminance: 0.2126*R + 0.7152*G + 0.0722*B (linearized) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fuse.js search unit tests</name>
  <files>test/shell.test.mjs</files>
  <behavior>
    - Test 1: Query "openai" matches a document whose title contains "OpenAI" (title weight 0.6)
    - Test 2: Query "inference" matches a document tagged "inference" even if title doesn't match (tag weight 0.3)
    - Test 3: Query "zzzznothing" returns 0 results (no false positives above threshold 0.35)
    - Test 4: Results are returned as an array (not null/undefined)
    - Test 5: Each result object has `item` property with `title`, `tags`, `summary` fields
  </behavior>
  <action>
    Create `test/shell.test.mjs` using Node.js built-in test runner (`import { test, describe } from 'node:test'` and `import { strict as assert } from 'node:assert'`).

    Import Fuse from 'fuse.js' (already installed at node_modules/fuse.js).

    Define a minimal sample dataset (3-5 entries) that covers the test cases:
    - One entry with title containing "OpenAI", tags: ["llm", "api"]
    - One entry with title "Vertex AI", tags: ["inference", "cloud"], summary: "Google cloud ML platform"
    - One entry with title "Anthropic", tags: ["safety", "llm"], summary: "AI safety research"

    Use the exact Fuse config from the research doc (keys with weights, threshold 0.35, includeScore: false).

    Tests must pass with `node --test test/shell.test.mjs`. No watch flags. No external test framework.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && node --test test/shell.test.mjs</automated>
  </verify>
  <done>All 5 test cases pass; `node --test test/shell.test.mjs` exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WCAG contrast unit tests</name>
  <files>test/contrast.test.mjs</files>
  <behavior>
    - Test 1: --text-primary (#D0D0D0) vs --bg-base (#0D0D1A) >= 4.5:1
    - Test 2: --neon-cyan (#00E5FF) vs --bg-base (#0D0D1A) >= 4.5:1
    - Test 3: --neon-magenta (#FF2D78) vs --bg-base (#0D0D1A) >= 4.5:1
    - Test 4: --neon-green (#39FF14) vs --bg-base (#0D0D1A) >= 4.5:1
    - Test 5: --text-secondary (#8888AA) vs --bg-base (#0D0D1A) >= 3.0:1 (large text / UI components threshold — secondary text is metadata, not body copy)
  </behavior>
  <action>
    Create `test/contrast.test.mjs` using Node.js built-in test runner.

    Implement a pure JS `contrastRatio(hex1, hex2)` function:
    1. Parse hex to RGB (0-255)
    2. Linearize: c <= 0.04045 ? c/12.92 : ((c+0.055)/1.055)^2.4 for each channel
    3. Luminance: L = 0.2126*R + 0.7152*G + 0.0722*B
    4. Ratio: (max(L1,L2) + 0.05) / (min(L1,L2) + 0.05)

    Hardcode the hex values from global.css :root (they are the ground truth). Do not read the CSS file at runtime.

    Note: --text-secondary at #8888AA has a lower contrast ratio — test it at >= 3.0:1 (large text AA threshold, appropriate for metadata labels). Research doc notes "lowest: neon-magenta 5.41:1" — all neons and primary text must pass 4.5:1.

    Tests must pass with `node --test test/contrast.test.mjs`.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && node --test test/contrast.test.mjs</automated>
  </verify>
  <done>All 5 contrast tests pass; `node --test test/contrast.test.mjs` exits 0</done>
</task>

</tasks>

<verification>
Both test files exist and pass:
```
node --test test/
```
Output shows all tests passing, exit code 0.
</verification>

<success_criteria>
- `test/shell.test.mjs` exists with 5 passing Fuse.js tests
- `test/contrast.test.mjs` exists with 5 passing contrast tests
- `node --test test/` exits 0
- No external test framework installed (Node built-in only)
</success_criteria>

<output>
After completion, create `.planning/phases/08-global-shell/08-01-SUMMARY.md`
</output>
