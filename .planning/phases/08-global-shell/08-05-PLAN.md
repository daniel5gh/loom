---
phase: 08-global-shell
plan: 05
type: execute
wave: 4
depends_on:
  - 08-04
files_modified: []
autonomous: false
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-05
  - SHELL-06
  - AESTH-01
  - AESTH-02
  - AESTH-03
  - AESTH-04
must_haves:
  truths:
    - "Pressing `/` opens the overlay and Esc closes it from any page"
    - "Fuzzy search returns relevant results for title, tag, and keyword queries"
    - "Arrow keys navigate results and Enter opens the selected document"
    - "j/k/gg/G/Enter navigate list items on index, tags/index, and tags/[tag] pages"
    - "Status bar is visible at the bottom of every page"
    - "Scanline CRT overlay is visible on all pages"
    - "h1 headings animate on page load"
    - "ASCII dividers render on index and tags pages"
  artifacts:
    - path: "src/layouts/Base.astro"
      provides: "Shell overlay + vim handler + status bar — verified by human"
    - path: "public/styles/global.css"
      provides: "All Phase 8 aesthetic CSS — verified by human"
  key_links: []
---

<objective>
Human verification of all Phase 8 interactive and aesthetic behaviors.

Purpose: Browser DOM, keyboard events, and visual effects cannot be verified by automated tests. This checkpoint confirms every SHELL and AESTH requirement works as specified before the phase is marked complete.
Output: Human sign-off on all 10 Phase 8 requirements.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-global-shell/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run final test suite</name>
  <files></files>
  <action>
    Run the full test suite to confirm all automated tests are green:
    ```
    node --test test/
    ```

    Then start the dev server for human verification:
    ```
    npm run dev
    ```

    Confirm the build starts without errors and note the local URL (typically http://localhost:4321).
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && node --test test/</automated>
  </verify>
  <done>All automated tests passing; dev server started</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 8 Global Shell — all interactive and aesthetic features:
    - SHELL-01/06: `/` opens terminal overlay, Esc closes it
    - SHELL-02: Fuzzy search matches title, tag, keyword
    - SHELL-03: Arrow keys navigate results, Enter opens document
    - SHELL-04: Vim j/k/gg/G/Enter navigation on list pages
    - SHELL-05: Status bar at page bottom
    - AESTH-01: Scanline/CRT overlay on all pages
    - AESTH-02: h1 typewriter animation on page load
    - AESTH-03: ASCII dividers on index and tags pages
    - AESTH-04: Body text contrast (automated — verified by test/contrast.test.mjs)
  </what-built>
  <how-to-verify>
    Open http://localhost:4321 (or whatever port `npm run dev` reports).

    **SHELL-01 + SHELL-06:**
    1. Press `/` — verify terminal overlay appears with `loom> _` prompt and blinking cursor
    2. Press Esc — verify overlay closes and focus returns to page
    3. Repeat from Tags page (http://localhost:4321/tags/) and a document page — confirm works everywhere

    **SHELL-02 + SHELL-03:**
    1. Press `/` to open overlay
    2. Type "openai" — verify results appear with document titles
    3. Type a tag name (e.g., "llm") — verify tag-matched results appear
    4. Type a nonsense string (e.g., "zzzzz") — verify "no results" message
    5. Press ArrowDown — verify second result is highlighted
    6. Press ArrowUp — verify first result is re-highlighted
    7. Press Enter on a highlighted result — verify navigation to that document

    **SHELL-04 + SHELL-05:**
    1. Go to http://localhost:4321 (index page)
    2. Verify status bar visible at bottom of page (shows "NORMAL")
    3. Press `j` — verify first doc card is highlighted with cyan outline; status shows "1/N"
    4. Press `j` again — verify second card highlighted
    5. Press `k` — verify back to first card
    6. Press `G` — verify last item highlighted
    7. Press `g` twice quickly (within 500ms) — verify first item highlighted
    8. Press Enter on highlighted item — verify navigation to that document
    9. Repeat j/k/Enter test on http://localhost:4321/tags/ (tag list)
    10. Click any tag, repeat on the tag detail page

    **AESTH-01:**
    1. On any page, look for horizontal scanlines overlaid on content — subtle dark lines every ~4px
    2. Verify scanlines don't block clicking (pointer-events: none)
    3. Verify scanlines appear on all 4 page types (index, tags, tag-detail, doc page)

    **AESTH-02:**
    1. Hard-reload (Ctrl+Shift+R) the index page
    2. Verify h1 "Loom" fades in with slight upward motion (heading-appear animation)
    3. Hard-reload a document page — verify doc title h1 animates

    **AESTH-03:**
    1. On http://localhost:4321 — verify ASCII divider (─────) between hero and first category section
    2. On http://localhost:4321/tags/ — verify ASCII divider (═════) between heading and tag list

    **Focus guard (important regression check):**
    1. Open map page (http://localhost:4321/map/)
    2. Click in the map search input
    3. Type "j" and "k" — verify letters appear in input (NOT vim navigation)
    4. Press `/` while focused in map search — verify overlay does NOT open
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors verified. Describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human confirmation that all 10 Phase 8 requirements are met as specified.
</verification>

<success_criteria>
- All SHELL-01 through SHELL-06 behaviors verified by human
- All AESTH-01 through AESTH-04 behaviors verified by human
- Focus guard confirmed: vim keys and `/` do not fire inside map search input
- Phase 8 marked complete in ROADMAP.md and STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/08-global-shell/08-05-SUMMARY.md`
</output>
