---
phase: 06-map-page-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
autonomous: false
requirements:
  - MAP-01
  - MAP-02

must_haves:
  truths:
    - "Dots are visually sharp on a Retina/HiDPI display (devicePixelRatio scaling confirmed working)"
    - "Hovering a dot shows a tooltip positioned near the cursor with title and tags visible"
    - "No dot is at the canvas origin — all 15 docs spread across the canvas area"
  artifacts:
    - path: "dist/map/index.html"
      provides: "Built map page served in browser"
  key_links:
    - from: "Browser canvas rendering"
      to: "HiDPI devicePixelRatio scaling"
      via: "Visual inspection: dots crisp at 2x zoom on Retina display"
      pattern: "n/a — manual verification"
---

<objective>
Human visual checkpoint: verify the /map page renders correctly in a real browser — sharp dots on HiDPI display, working hover tooltip, and all 15 documents visible as spread-out dots.

Purpose: MAP-01 and MAP-02 have visual/interactive properties (HiDPI sharpness, tooltip DOM rendering) that cannot be automated with Node.js unit tests. This checkpoint closes the phase with human confirmation.
Output: Approved /map page or documented issues for gap closure.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-map-page-skeleton/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run full verification suite</name>
  <files></files>
  <action>
    Run the full automated test suite to confirm a clean baseline before manual inspection:

    node /home/daniel/codes/loom/src/lib/map.test.mjs && cd /home/daniel/codes/loom && npm run build

    Then start the Astro dev server:

    cd /home/daniel/codes/loom && npm run dev

    The dev server URL is typically http://localhost:4321. Report the URL to the user.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && node src/lib/map.test.mjs && npm run build 2>&1 | grep -E "build|error|warn" | tail -10</automated>
  </verify>
  <done>Full suite exits 0. Dev server is running and URL reported to user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — dots, HiDPI sharpness, tooltip</name>
  <files></files>
  <action>
    Pause for human verification. Open http://localhost:4321/map/ and verify the following:

    Check 1 — Dot layout (MAP-01):
    - 15 dots spread across the canvas (not all at origin or one corner)
    - Colored: cyan for ai-tools-and-services, pink for cloud-ai-platforms, green for companies
    - Each dot has a subtle neon glow effect

    Check 2 — HiDPI sharpness (MAP-01):
    - Zoom to 200% or view on Retina display — dots must be crisp, not blurry

    Check 3 — Hover tooltip (MAP-02):
    - Hovering a dot shows title (bold) and comma-separated tags in a tooltip near the cursor
    - Moving off hides the tooltip

    Check 4 — Nav link:
    - Site nav shows: Loom | Tags | Graph | Map

    Check 5 — Build correctness:
    - npm run build completed with 0 errors (confirmed Task 1)
  </action>
  <verify>
    <automated>MISSING — visual/interactive verification requires browser</automated>
  </verify>
  <done>Human types "approved" confirming all 5 checks pass, or describes failures for gap closure.</done>
  <what-built>
    - src/pages/map.astro: Canvas page reading embeddings.json at build time, 15 dots with neon glow, HiDPI scaling, tooltip
    - src/lib/map.test.mjs: Unit tests for coordinate normalization and hit detection (passing)
    - public/styles/global.css: Map layout and tooltip CSS
    - src/layouts/Base.astro: /map nav link
  </what-built>
  <how-to-verify>
    Open http://localhost:4321/map/ — complete all 5 checks listed in the action above.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass. Otherwise describe which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 6 complete when:
1. node src/lib/map.test.mjs exits 0
2. npm run build exits 0
3. Human confirms all 5 visual checks in the checkpoint above
</verification>

<success_criteria>
All Phase 6 success criteria from ROADMAP.md confirmed:
1. Visiting /map shows a canvas with one dot per document positioned by semantic similarity — MAP-01
2. Hovering a dot shows a tooltip with the document's title and tags — MAP-02
3. Dots are sharp on a Retina/HiDPI display — MAP-01
4. npm run build completes without SSR errors — MAP-01
</success_criteria>

<output>
After completion, create .planning/phases/06-map-page-skeleton/06-02-SUMMARY.md
</output>
