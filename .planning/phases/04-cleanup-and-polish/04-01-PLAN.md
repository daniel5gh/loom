---
phase: 04-cleanup-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-output.mjs
  - public/styles/global.css
  - wrangler.toml
autonomous: false
requirements:
  - REQ-006
  - REQ-030
  - REQ-043

must_haves:
  truths:
    - "Running `node scripts/validate-output.mjs` after a build fails with an error if dist/graph/index.html is absent"
    - "Document pages show a single 2rem padding from .doc-layout — main container adds no additional padding"
    - "`npx wrangler pages dev` (no positional argument) starts the local preview server and serves from dist/"
  artifacts:
    - path: "scripts/validate-output.mjs"
      provides: "Graph page existence assertion"
      contains: "existsSync(join(DIST, 'graph', 'index.html'))"
    - path: "public/styles/global.css"
      provides: "doc-layout padding override"
      contains: "main:has(.doc-layout)"
    - path: "wrangler.toml"
      provides: "Pages build output directory config"
      contains: "pages_build_output_dir = \"dist\""
  key_links:
    - from: "scripts/validate-output.mjs"
      to: "dist/graph/index.html"
      via: "existsSync check before summary block"
      pattern: "existsSync.*graph.*index\\.html"
    - from: "public/styles/global.css main:has(.doc-layout)"
      to: "public/styles/global.css .doc-layout"
      via: "CSS :has() override zeroing main padding so .doc-layout padding applies alone"
      pattern: "main:has\\(\\.doc-layout\\)"
    - from: "wrangler.toml pages_build_output_dir"
      to: "dist/"
      via: "Wrangler 3.x reads key to determine serve directory without positional arg"
      pattern: "pages_build_output_dir"
---

<objective>
Close three discrete tech debt items (MC-01, MC-02, MC-04) identified in the v1.0 milestone audit.

Purpose: These are known gaps in an otherwise complete v1.0. Each fix is a localized edit to a single file. Closing them brings the codebase to a clean state with no outstanding audit findings.
Output: Updated validate-output.mjs (graph page assertion), updated global.css (layout padding fix), updated wrangler.toml (pages_build_output_dir key).
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup-and-polish/04-RESEARCH.md

<interfaces>
<!-- Key implementation details extracted from current codebase. Executor should use these directly. -->

From scripts/validate-output.mjs (lines 51-57) — existing index.html check pattern to mirror:
```javascript
// Check index page exists
if (!existsSync(join(DIST, 'index.html'))) {
  errors.push('Missing dist/index.html');
} else {
  console.log('OK: dist/index.html exists');
  passed++;
}

// Spot-check a document page...  <-- insertion point for graph check
```

From public/styles/global.css — existing override pattern (mirror for doc-layout fix):
```css
/* Line ~415 — graph page override (pattern to mirror) */
main:has(.graph-page) {
  max-width: none;
  margin: 0;
  padding: 0;
}
/* Insert doc-layout override immediately after this block */
```

Current wrangler.toml (full file):
```toml
compatibility_date = "2025-07-18"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add graph page assertion to validate-output.mjs</name>
  <files>scripts/validate-output.mjs</files>
  <action>
Insert a graph page existence check after the existing `dist/index.html` check block (after line 57, before the "Spot-check a document page" comment at line 59). Use the identical pattern to the index.html check:

```javascript
// Check graph page exists
if (!existsSync(join(DIST, 'graph', 'index.html'))) {
  errors.push('Missing dist/graph/index.html');
} else {
  console.log('OK: dist/graph/index.html exists');
  passed++;
}
```

IMPORTANT: The insertion must occur BEFORE the summary block at line 102 (`console.log(\`\nValidation:...\`)`). Placing it after the summary would allow the check to run but print misleading results.

Do NOT alter any existing checks, counters, or the exit logic.
  </action>
  <verify>
    <automated>npm run build && node scripts/validate-output.mjs</automated>
  </verify>
  <done>Running `npm run build && node scripts/validate-output.mjs` outputs "OK: dist/graph/index.html exists" and exits 0. If the graph page were absent the script would exit 1 with "Missing dist/graph/index.html".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix double padding on document pages</name>
  <files>public/styles/global.css</files>
  <action>
Add a CSS override immediately after the existing `main:has(.graph-page)` block (around line 419):

```css
/* Remove main container padding when doc-layout is present (avoids double-padding with .doc-layout's own padding) */
main:has(.doc-layout) {
  padding: 0;
}
```

CRITICAL constraints:
- Only set `padding: 0`. Do NOT add `max-width: none` or `margin: 0` — those would conflict with `.doc-layout`'s own centering rules and are not needed.
- Do NOT edit the base `main { padding: 2rem }` rule — it is intentional for pages that use neither `.graph-page` nor `.doc-layout`.
- Do NOT edit `.doc-layout { padding: 2rem 1rem }` directly — the media query at line 350 depends on that padding for single-column layout on narrow viewports.
  </action>
  <verify>
    <automated>npm run build && node scripts/validate-output.mjs</automated>
  </verify>
  <done>CSS file contains `main:has(.doc-layout) { padding: 0; }` placed after the `main:has(.graph-page)` block. Build passes. Visual check: opening a document page in the browser shows the content is not indented by an outer 2rem from `main` — only the inner `.doc-layout` padding is visible.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add pages_build_output_dir to wrangler.toml</name>
  <files>wrangler.toml</files>
  <action>
Add `pages_build_output_dir = "dist"` to wrangler.toml. The complete file after the edit:

```toml
compatibility_date = "2025-07-18"
pages_build_output_dir = "dist"
```

This is the entire file — no other content. Do not add any other keys, comments, or sections.

After the fix, `npx wrangler pages dev` (with no positional argument) will read this key and serve from `dist/` automatically.

NOTE: Do not update any documentation or scripts that currently pass `dist` as a positional argument — that is out of scope for this plan.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const toml = fs.readFileSync('wrangler.toml', 'utf8'); if (!toml.includes('pages_build_output_dir')) { console.error('MISSING pages_build_output_dir'); process.exit(1); } else { console.log('OK: pages_build_output_dir present'); }"</automated>
  </verify>
  <done>wrangler.toml contains `pages_build_output_dir = "dist"`. Running `npx wrangler pages dev` (no args) starts the local preview server and serves from dist/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual and functional verification</name>
  <files>none</files>
  <action>Pause for human verification of the three automated fixes.</action>
  <verify>Human confirms all three success criteria below.</verify>
  <done>All three audit items confirmed closed by human inspection.</done>
  <what-built>
Three tech debt fixes: (1) validate-output.mjs now checks for dist/graph/index.html, (2) global.css has main:has(.doc-layout) { padding: 0 } preventing double padding on document pages, (3) wrangler.toml has pages_build_output_dir = "dist".
  </what-built>
  <how-to-verify>
1. Confirm build + validator are green: `npm run build && node scripts/validate-output.mjs` — should show "OK: dist/graph/index.html exists" in output and exit 0.

2. Open a document page in browser (e.g., http://localhost:4321/ai-tools-and-services/airllm/ after `npm run dev`). Using DevTools, inspect the `main` element — padding should be 0px from `main` itself; the visible spacing comes from `.doc-layout { padding: 2rem 1rem }` inside. Total vertical offset from viewport edge to content should be ~2rem (not ~4rem).

3. Run `npx wrangler pages dev` (no arguments). Confirm: server starts, output shows it is serving from `dist/`, and pages load at http://localhost:8788.
  </how-to-verify>
  <resume-signal>Type "approved" if all three checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build && node scripts/validate-output.mjs` exits 0 and includes "OK: dist/graph/index.html exists"
- `public/styles/global.css` contains `main:has(.doc-layout) { padding: 0; }` after the graph page override
- `wrangler.toml` contains `pages_build_output_dir = "dist"`
- No regressions: all previously-passing validator checks still pass
</verification>

<success_criteria>
All three Phase 4 audit items closed:
1. MC-01 closed: validator asserts dist/graph/index.html and fails the build if it is missing
2. MC-02 closed: document pages have no double padding — main contributes 0 padding when .doc-layout is present
3. MC-04 closed: `npx wrangler pages dev` (no positional argument) starts correctly using pages_build_output_dir from wrangler.toml
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-and-polish/04-01-SUMMARY.md` following the summary template.
</output>
