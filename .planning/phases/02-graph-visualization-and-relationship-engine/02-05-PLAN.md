---
phase: 02-graph-visualization-and-relationship-engine
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-04
files_modified: []
autonomous: false
requirements:
  - REQ-020
  - REQ-021
  - REQ-022
  - REQ-023
  - REQ-024
  - REQ-025
  - REQ-026
  - REQ-014

must_haves:
  truths:
    - "All graph interactive behaviors verified in a real browser"
    - "Related Documents sidebar verified on a live document page"
    - "Phase 2 is complete and ready for /gsd:verify-work"
  artifacts:
    - path: "dist/"
      provides: "Built site with all Phase 2 features"
  key_links: []
---

<objective>
Human visual checkpoint: verify all interactive graph behaviors and the Related Documents sidebar work correctly in a browser before marking Phase 2 complete.

Purpose: D3 interactions cannot be verified by automated build checks — they require a human with a browser. This checkpoint confirms the phase goal before calling /gsd:verify-work.

Output: Phase 2 sign-off from user.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-graph-visualization-and-relationship-engine/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of Phase 2 interactive features</name>
  <action>Human verifies all interactive behaviors in a browser. Run `npm run dev` to start the dev server, then follow the verification checklist below.</action>
  <verify>
    <automated>cd /home/daniel/codes/loom && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User types "approved" after verifying all checklist items.</done>
  <what-built>
    Complete Phase 2 implementation:
    - /graph/ page: D3 force-directed graph with 15 document nodes, edges for 2+ shared tags, neon styling, zoom/pan, hover highlighting, click navigation, tag filter sidebar
    - All document pages: Related Documents sidebar with title links and shared tag names
    - Base.astro: Graph nav link added
  </what-built>
  <how-to-verify>
    Run the dev server first: `npm run dev` (or `npm run build && npx wrangler pages dev dist`)

    1. GRAPH PAGE — visit http://localhost:4321/graph/
       - [ ] 15 document nodes visible as colored circles with text labels
       - [ ] Edges visible connecting nodes that share 2+ tags
       - [ ] Node circles have different sizes (larger = more tags)
       - [ ] Nodes have 3 distinct colors (cyan, magenta, green) + gray for unclustered
       - [ ] Labels are visible at default zoom level
       - [ ] Scroll wheel zooms in/out; drag on empty space pans
       - [ ] Zooming out far (scroll out multiple times) hides node labels
       - [ ] Hovering a node: that node and its neighbors stay bright; all other nodes dim to near-invisible
       - [ ] Moving mouse off a node: all nodes return to full opacity
       - [ ] Clicking a node: browser navigates to that document's page URL

    2. TAG FILTER — on the /graph/ page
       - [ ] Left sidebar lists tags with document counts
       - [ ] Clicking a tag: only nodes with that tag remain bright; others dim
       - [ ] Clicking the same tag again: all nodes return to full opacity
       - [ ] Active tag button has a highlighted style

    3. RELATED DOCUMENTS — visit any document page (e.g., http://localhost:4321/ai-tools-and-services/langchain/)
       - [ ] "Related" sidebar visible to the right of the document content
       - [ ] 1-5 related documents listed, each as a clickable link
       - [ ] Each entry shows "shared: tag1, tag2" below the title
       - [ ] Sidebar is sticky (stays visible as you scroll the main content)

    4. NAVIGATION
       - [ ] All pages show "Graph" link in the nav bar
       - [ ] Clicking "Graph" nav link goes to /graph/
  </how-to-verify>
  <resume-signal>Type "approved" if all items verified, or describe any issues found so they can be fixed.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all interactive behaviors listed in how-to-verify.
</verification>

<success_criteria>
- User types "approved" after verifying all checklist items
- Phase 2 ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create .planning/phases/02-graph-visualization-and-relationship-engine/02-05-SUMMARY.md
</output>
