---
phase: 03-claude-code-skills-and-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
requirements:
  - REQ-016
  - REQ-033
  - REQ-043
  - REQ-050
  - REQ-051
  - REQ-052
  - REQ-053

must_haves:
  truths:
    - "All Phase 3 requirements are visually verified in a running browser"
    - "Syntax highlighting renders with neon token colors on code blocks"
    - "Glow effects appear on tag pills and doc cards on hover"
    - "All three Claude Code skills appear in the slash command menu"
    - "wrangler pages dev serves the site without errors"
  artifacts:
    - path: ".claude/skills/research/SKILL.md"
      provides: "/research command confirmed accessible"
    - path: ".claude/skills/organize/SKILL.md"
      provides: "/organize command confirmed accessible"
    - path: ".claude/skills/validate/SKILL.md"
      provides: "/validate command confirmed accessible"
  key_links:
    - from: "browser visual check"
      to: "all Phase 3 success criteria"
      via: "human verification at localhost:8788"
      pattern: "checkpoint:human-verify"
---

<objective>
Human visual verification of all Phase 3 deliverables: syntax highlighting, glow effects, wrangler preview, and Claude Code skill accessibility.

Purpose: Phase gate — confirms all v1 requirements for Phase 3 are met before marking the milestone complete.
Output: Human-approved verification that Phase 3 is done.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-claude-code-skills-and-polish/03-01-SUMMARY.md
@.planning/phases/03-claude-code-skills-and-polish/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch Wrangler local preview</name>
  <files></files>
  <action>
    Run the full build and start the Wrangler local preview:
    ```bash
    cd /home/daniel/codes/loom
    npm run build && node scripts/validate-output.mjs
    ```
    Then start wrangler:
    ```bash
    npx wrangler pages dev dist/ --port 8788
    ```
    Wrangler serves the site at http://127.0.0.1:8788.

    Note: Wrangler is a long-running process. Start it in the background and print the URL for the user to open.

    If wrangler fails to start (e.g., port in use), try port 8789 or use `npm run dev` (Astro dev server) as the fallback for visual inspection.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && npm run build && node scripts/validate-output.mjs 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds, validate-output.mjs passes all checks, and wrangler starts serving dist/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01: Shiki syntax highlighting (css-variables theme mapped to neon palette) + CSS neon glow on tag pills and doc cards + wrangler preview workflow.
    Plan 02: Three Claude Code skills — /research, /organize, /validate — all enforcing tag normalization.
  </what-built>
  <how-to-verify>
    Open http://127.0.0.1:8788 (or npm run dev at http://localhost:4321 if wrangler is unavailable).

    **REQ-016 — Syntax highlighting:**
    1. Navigate to any document page that contains a fenced code block (try ai-tools-and-services/ pages).
    2. Verify code tokens are colored: keywords in cyan (#00E5FF), strings in green (#39FF14), comments in grey (#8888AA), functions in magenta (#FF2D78).
    3. Verify the code block has a dark background matching the page palette.

    **REQ-033 — Glow effects:**
    4. On any document page, hover a tag pill. Verify a neon cyan glow/bloom appears around the border (not on the text itself).
    5. On the index page, hover a document card. Verify a neon cyan glow appears around the card border.
    6. Verify no glow appears on paragraph text or headings (would indicate a WCAG regression).

    **REQ-043 — Wrangler preview:**
    7. Confirm the site loaded at http://127.0.0.1:8788 without error pages.

    **REQ-050/051/052/053 — Skills:**
    8. In Claude Code, type `/` and verify `/research`, `/organize`, and `/validate` appear in the slash command menu.
    9. (Optional quick test) Run `/validate` — verify it lists documents and reports any issues without modifying files.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All Phase 3 success criteria (from ROADMAP.md) must be TRUE:
1. /research creates valid document with normalized tags
2. /organize audits tags and reports issues without auto-modifying
3. /validate reports frontmatter errors without modifying files
4. Code blocks render with syntax highlighting
5. Graph nodes and accent elements display glow effects consistent with neon aesthetic
</verification>

<success_criteria>
- Build passes: `npm run build && node scripts/validate-output.mjs` exits 0
- Human confirms syntax highlighting renders with neon token colors
- Human confirms glow effects on tag pills and doc cards
- Human confirms wrangler preview serves the site
- Human confirms all three skills appear in Claude Code slash command menu
- Phase 3 marked complete in ROADMAP.md progress table
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-skills-and-polish/03-03-SUMMARY.md`
</output>
