---
phase: 03-claude-code-skills-and-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified: []
autonomous: false
requirements:
  - REQ-016
  - REQ-033
  - REQ-043
  - REQ-050
  - REQ-051
  - REQ-052
  - REQ-053

must_haves:
  truths:
    - "Build completes successfully with Shiki enabled"
    - "validate-output.mjs passes all checks after build"
    - "All three skill files exist with correct frontmatter"
    - "Wrangler serves the built site at localhost:8788"
    - "Syntax highlighting renders with neon token colors in browser"
    - "Tag pill and doc card hover glow is visible in browser"
  artifacts:
    - path: ".claude/skills/research/SKILL.md"
      provides: "/loom:research command"
      contains: "name: loom:research"
    - path: ".claude/skills/organize/SKILL.md"
      provides: "/loom:organize command"
      contains: "name: loom:organize"
    - path: ".claude/skills/validate/SKILL.md"
      provides: "/loom:validate command"
      contains: "name: loom:validate"
    - path: "astro.config.mjs"
      provides: "Shiki syntax highlighting"
      contains: "shikiConfig"
    - path: "public/styles/global.css"
      provides: "Neon glow effects and Shiki token colors"
      contains: "--astro-code-color-background"
  key_links:
    - from: "astro.config.mjs"
      to: "public/styles/global.css"
      via: "css-variables theme delegates token colors to --astro-code-* CSS vars"
      pattern: "astro-code-token-keyword"
---

<objective>
Verify the complete Phase 3 implementation: automated build checks, skill file structure checks, and a human visual confirmation of syntax highlighting and glow effects in the browser.

Purpose: Gate before marking Phase 3 complete — confirms all automated and visual requirements are met.
Output: Confirmed working Shiki highlighting, glow effects, and three registered Claude Code skills.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-code-skills-and-polish/03-01-SUMMARY.md
@.planning/phases/03-claude-code-skills-and-polish/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build and structure verification</name>
  <files></files>
  <action>
    Run the full verification suite and report results. Do not modify any files in this task.

    Run in order:

    1. Build check:
       ```bash
       cd /home/daniel/codes/loom && npm run build
       ```
       Must exit 0. If it fails, surface the error — do not attempt to fix here.

    2. Output validation:
       ```bash
       node scripts/validate-output.mjs
       ```
       Must pass all checks. The astro-code check is INFO-level and acceptable to skip if no docs have code blocks.

    3. Skill structure checks:
       ```bash
       grep "name: loom:research" .claude/skills/research/SKILL.md
       grep "name: loom:organize" .claude/skills/organize/SKILL.md
       grep "name: loom:validate" .claude/skills/validate/SKILL.md
       grep "disable-model-invocation" .claude/skills/research/SKILL.md .claude/skills/organize/SKILL.md .claude/skills/validate/SKILL.md
       grep "toLowerCase" .claude/skills/research/SKILL.md .claude/skills/organize/SKILL.md .claude/skills/validate/SKILL.md
       ```
       All must return matches.

    4. CSS checks:
       ```bash
       grep "shikiConfig" astro.config.mjs
       grep -- "--astro-code-color-background" public/styles/global.css
       grep "box-shadow" public/styles/global.css
       ```
       All must return matches.

    Report pass/fail for each check. Stop and surface any failures before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && npm run build && node scripts/validate-output.mjs && grep "name: loom:research" .claude/skills/research/SKILL.md && grep "name: loom:organize" .claude/skills/organize/SKILL.md && grep "name: loom:validate" .claude/skills/validate/SKILL.md && echo "ALL AUTOMATED CHECKS PASSED"</automated>
  </verify>
  <done>npm run build exits 0, validate-output.mjs passes, all three skill name fields confirmed, CSS variables and glow rules confirmed present</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of glow effects and syntax highlighting</name>
  <files></files>
  <action>
    Pause for human visual confirmation. Run local preview first:
      npm run build
      npx wrangler pages dev dist/
    Then verify glow effects and syntax highlighting in the browser at http://127.0.0.1:8788.
  </action>
  <verify>
    <automated>MISSING — visual checks require human browser verification</automated>
  </verify>
  <done>Human confirms glow effects visible on hover, syntax highlighting active with neon colors, wrangler preview runs at port 8788</done>
  <what-built>
    Phase 3 complete:
    - Shiki syntax highlighting via Astro css-variables theme, neon token colors in global.css
    - CSS neon glow effects on tag pill and doc card hover states
    - Three Claude Code skills: /loom:research, /loom:organize, /loom:validate
    - validate-output.mjs extended with Shiki smoke check

    Start local preview:
    npm run build
    npx wrangler pages dev dist/
    (serves at http://127.0.0.1:8788)
  </what-built>
  <how-to-verify>
    1. Open http://127.0.0.1:8788 in browser.

    2. Syntax highlighting (REQ-016):
       - Open any document page that contains a code block (e.g., check ai-tools-and-services/ for docs with code)
       - Verify code blocks render with colored tokens: cyan keywords, green strings, magenta functions
       - If no docs have code blocks, this check is N/A (Shiki fires on code blocks only)

    3. Glow effects (REQ-033):
       - On any page with tag pills: hover over a tag pill
       - Verify: a neon-cyan glow bloom appears around the pill border
       - On the home page or any page with doc cards: hover over a card
       - Verify: a neon-cyan glow bloom appears around the card border
       - Verify: body text has NO glow (no text-shadow on paragraphs or headings)

    4. Wrangler local preview (REQ-043):
       - Site loads at http://127.0.0.1:8788 without errors
       - Navigation works (doc pages, tag pages, graph page)

    5. Skills visible (REQ-050/051/052):
       - In Claude Code, type "/" — verify /loom:research, /loom:organize, /loom:validate appear
       - (Only verifiable in Claude Code interface itself, not the static site)
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass, or describe any issues (e.g., "glow missing on cards", "syntax colors wrong")</resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
- npm run build exits 0
- node scripts/validate-output.mjs passes
- All skill name field greps return matches
- Human confirms glow effects visible and wrangler serves correctly
</verification>

<success_criteria>
Phase 3 success (all must be TRUE):
1. /loom:research, /loom:organize, /loom:validate registered as Claude Code slash commands
2. All three skills enforce tag normalization rule (tag.toLowerCase().trim().replace(/\s+/g, '-'))
3. Code blocks in built HTML pages have Shiki markup (pre.astro-code) with neon token colors
4. Tag pills and doc cards show neon-cyan glow on hover in browser
5. npm run build and validate-output.mjs both pass
6. npx wrangler pages dev dist/ serves the site at port 8788
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-skills-and-polish/03-03-SUMMARY.md`
</output>
