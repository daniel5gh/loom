---
phase: 01-content-pipeline-and-site-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
autonomous: false
files_modified: []
requirements:
  - REQ-040
  - REQ-041
  - REQ-042

must_haves:
  truths:
    - "A Cloudflare Pages project exists connected to the GitHub repo"
    - "Pushing to main triggers a Cloudflare Pages build that produces a live URL"
    - "The live URL renders all 15 document pages, tag navigation, and the dark/neon theme"
    - "git status after npm run build shows no untracked files in dist/"
  artifacts:
    - path: ".gitignore"
      provides: "dist/ excluded from git (verified by git status)"
  key_links:
    - from: "GitHub repo (main branch)"
      to: "Cloudflare Pages project"
      via: "Git integration configured in CF dashboard"
      pattern: "n/a — dashboard config"
---

<objective>
Connect the repo to Cloudflare Pages and verify that a git push triggers a live build, then do a visual checkpoint of the deployed site.

Purpose: REQ-041 (git-based auto-deploy) requires a live connection between the repo and Cloudflare Pages. This cannot be automated — it requires logging into the Cloudflare dashboard. After the checkpoint, verify the deployed URL renders correctly.
Output: Live Cloudflare Pages URL with working auto-deploy. Phase 1 complete.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-pipeline-and-site-foundation/01-03-SUMMARY.md
@.planning/phases/01-content-pipeline-and-site-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build is clean and commit all changes</name>
  <files></files>
  <action>
Run the full build suite and confirm git cleanliness before deployment.

Run full build and output validation:
```bash
cd /home/daniel/codes/loom && npm run build && node scripts/validate-output.mjs
```
Expected: exits 0, reports 15 document pages, tag pages present, tag links confirmed.

Verify .gitignore excludes dist/ by running git status after build. dist/ must NOT appear in the output.

Confirm .node-version contains 22.

If build fails or git status shows dist/ tracked, fix the issue before proceeding.

Commit all final changes to main branch before the checkpoint.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && npm run build 2>&1 | tail -3 && node scripts/validate-output.mjs 2>&1 && git status --short 2>&1 | python3 -c "import sys; lines = sys.stdin.read(); dist_lines = [l for l in lines.splitlines() if 'dist/' in l and not l.startswith('??'+'  .planning')]; print('dist/ in git status:', dist_lines if dist_lines else 'none (OK)')"</automated>
  </verify>
  <done>
npm run build and validate-output.mjs both exit 0. git status shows dist/ is not tracked. .node-version contains 22. All changes committed to main.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Connect repo to Cloudflare Pages</name>
  <files></files>
  <action>
Connect the GitHub repo to Cloudflare Pages using the Cloudflare dashboard.

Steps:
1. Log in to https://dash.cloudflare.com
2. Navigate to: Workers and Pages > Pages > Create a project > Connect to Git
3. Authorize GitHub access and select the loom repository
4. Configure build settings:
   - Framework preset: None (or Astro if available)
   - Build command: npm run build
   - Build output directory: dist
   - Root directory: / (repo root)
   - Node.js version: leave as default (build image v3 uses Node 22 matching .node-version)
5. Click Save and Deploy
6. Wait for first build to complete in the CF dashboard
7. Copy the generated *.pages.dev URL

After getting the URL, update astro.config.mjs — replace https://loom.pages.dev with the actual URL, commit, and push to main. Verify the second build also completes.
  </action>
  <verify>
    <automated>MISSING — requires Cloudflare dashboard interaction; verified via resume-signal</automated>
  </verify>
  <done>Cloudflare Pages project is connected to the GitHub repo and the first build completes with a live *.pages.dev URL.</done>
  <resume-signal>
Type "deployed: https://YOUR-PROJECT.pages.dev" with the actual Cloudflare Pages URL when the first build completes and the site is live.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of the live deployed site</name>
  <files></files>
  <action>
Visit the Cloudflare Pages URL and verify the full site works correctly.
  </action>
  <verify>
    <automated>MISSING — visual verification requires a browser; verified via resume-signal</automated>
  </verify>
  <done>All 5 visual checks confirmed: dark theme, Fira Code font, three category sections, tag navigation, document pages render correctly.</done>
  <what-built>
Complete Loom site: 15 document pages, tag navigation, dark/neon aesthetic, live on Cloudflare Pages with git auto-deploy.
  </what-built>
  <how-to-verify>
Visit the Cloudflare Pages URL and verify:

1. Index page (/): dark background, Fira Code monospace font, three category sections (AI Tools and Services, Cloud AI Platforms, Companies), all 15 document cards visible, nav bar with Loom and Tags links.

2. Document page (click any card): title as H1, date displayed, tag pills with neon cyan border, clicking a tag pill navigates to /tags/{tag}/, body content renders.

3. Tag page (click any tag): lists all documents sharing that tag, each links back to its page.

4. Tags index (click Tags in nav): all unique tags listed, each links to its tag page.

5. Auto-deploy test (optional): make a trivial commit, push to main, confirm new build triggers in CF dashboard.
  </how-to-verify>
  <resume-signal>
Type "verified" if all checks pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. npm run build and node scripts/validate-output.mjs both exit 0
2. Live Cloudflare Pages URL serves the site
3. All 5 visual checks pass at the deployed URL
4. A git push triggers an automatic rebuild (REQ-041)
</verification>

<success_criteria>
- Static HTML output in dist/ after npm run build (REQ-040)
- Cloudflare Pages auto-deploy: push to main triggers build and publish (REQ-041)
- .gitignore excludes dist/ and node_modules/ confirmed by git status (REQ-042)
- All Phase 1 success criteria from ROADMAP.md confirmed at the live URL
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-pipeline-and-site-foundation/01-05-SUMMARY.md`
</output>
