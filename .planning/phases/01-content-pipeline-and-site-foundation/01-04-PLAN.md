---
phase: 01-content-pipeline-and-site-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - public/styles/global.css
autonomous: true
requirements:
  - REQ-030
  - REQ-031
  - REQ-032
  - REQ-034

must_haves:
  truths:
    - "All pages have a dark background (#0D0D1A) applied via body CSS"
    - "Fira Code monospace font loads and applies to body and nav elements"
    - "Tag pills have neon cyan (#00E5FF) border and text — not used as body text color"
    - "Body text (#D0D0D0) on dark background achieves minimum 4.5:1 WCAG AA contrast ratio"
    - "Neon accent colors appear only on interactive elements (tag pills, links, hover states) — never on body text or headings"
  artifacts:
    - path: "public/styles/global.css"
      provides: "All CSS custom properties, dark theme, Fira Code font import, component styles for TagPill, DocCard, layouts"
  key_links:
    - from: "src/layouts/Base.astro"
      to: "public/styles/global.css"
      via: "<link rel='stylesheet' href='/styles/global.css' />"
      pattern: "styles/global.css"
---

<objective>
Create the complete CSS stylesheet implementing the dark/neon aesthetic across all pages.

Purpose: Plan 03 (parallel wave) builds all routes and components without styles. This plan creates `public/styles/global.css` which Base.astro already references via `<link rel="stylesheet" href="/styles/global.css" />`. When both Wave 3 plans complete, the site is functional AND styled.
Output: `public/styles/global.css` — one file, all styles.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-content-pipeline-and-site-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Color palette from 01-RESEARCH.md — use these exact values -->
<!-- DO NOT substitute other colors — these are locked by research verification -->

CSS custom properties:
  --bg-base:        #0D0D1A   /* Deep dark — main background */
  --bg-surface:     #13132A   /* Cards, sidebar */
  --bg-elevated:    #1A1A3A   /* Code blocks, hover states */

  --text-primary:   #D0D0D0   /* Body text — ~10:1 contrast on --bg-base */
  --text-secondary: #8888AA   /* Metadata, dates, secondary labels */
  --text-dim:       #555577   /* Disabled, placeholder */

  --neon-cyan:      #00E5FF   /* Primary accent — tag pills, links, active states */
  --neon-magenta:   #FF2D78   /* Secondary — hover states, highlights */
  --neon-green:     #39FF14   /* Tertiary — category labels */

  --font-mono: 'Fira Code', 'Cascadia Code', monospace

WCAG AA rules (from research):
- Body text (#D0D0D0) on --bg-base (#0D0D1A): ~10:1 ratio — passes AA
- Neon colors on dark: ONLY use on borders, underlines, background of badges — NOT on text
- Headings: use --text-primary, NOT neon colors

Fira Code import (self-hosted via @fontsource):
  @import '@fontsource/fira-code/400.css';
  @import '@fontsource/fira-code/600.css';

HTML class names used in Plan 03 components (match these exactly):
  Layout: .site-nav, .nav-home, .nav-tags
  Index: .index-hero, .index-title, .index-subtitle, .category-section, .category-heading, .doc-grid
  DocCard: .doc-card, .doc-card-link, .doc-card-title, .doc-card-date, .doc-card-tags
  Document: .doc-page, .doc-header, .doc-title, .doc-date, .doc-tags, .doc-content
  Tags: .tag-pill, .tag-list, .tag-count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global.css with dark theme, typography, and component styles</name>
  <files>public/styles/global.css</files>
  <action>
Create `public/styles/global.css`. This is the single stylesheet for the entire site. It must be placed in `public/styles/` (not `src/styles/`) because Base.astro references it as a static asset via `/styles/global.css`.

The stylesheet must include all of the following sections:

**1. Font imports** (Fira Code from @fontsource, self-hosted):
```css
@import '@fontsource/fira-code/400.css';
@import '@fontsource/fira-code/600.css';
```

**2. CSS custom properties on :root** — use EXACT color values from research (see interfaces above).

**3. CSS reset / base**:
- `box-sizing: border-box` on `*`
- `margin: 0; padding: 0` reset
- `body` background: `var(--bg-base)`, color: `var(--text-primary)`, font-family: `var(--font-mono)`, line-height: 1.6
- `a` color: `var(--neon-cyan)`, no underline by default, underline on hover

**4. Navigation (.site-nav)**:
- Flexbox row with gap
- Border-bottom: 1px solid `var(--bg-elevated)`
- Padding: 1rem 2rem
- `.nav-home`: font-weight 600, color `var(--neon-cyan)`, no underline
- `.nav-tags`: color `var(--text-secondary)`

**5. Main content area**:
- `main` max-width 1100px, margin 0 auto, padding 2rem

**6. Index page**:
- `.index-hero`: padding 2rem 0
- `.index-title`: font-size 2.5rem, font-weight 600, color `var(--text-primary)`
- `.index-subtitle`: color `var(--text-secondary)`, margin-top 0.5rem
- `.category-section`: margin-top 3rem
- `.category-heading`: font-size 1.25rem, color `var(--neon-green)`, text-transform uppercase, letter-spacing 0.1em, padding-bottom 0.5rem, border-bottom 1px solid `var(--bg-elevated)`, margin-bottom 1rem
- `.doc-grid`: display grid, grid-template-columns repeat(auto-fill, minmax(280px, 1fr)), gap 1rem

**7. DocCard (.doc-card)**:
- Background: `var(--bg-surface)`, border 1px solid `var(--bg-elevated)`, border-radius 6px, padding 1rem
- Hover: border-color `var(--neon-cyan)`, transition 0.15s
- `.doc-card-link`: text-decoration none, display block
- `.doc-card-title`: color `var(--text-primary)`, font-size 1rem, font-weight 600, margin-bottom 0.25rem
- `.doc-card-title:hover`: color `var(--neon-cyan)`
- `.doc-card-date`: color `var(--text-secondary)`, font-size 0.8rem, display block, margin-bottom 0.5rem
- `.doc-card-tags`: display flex, flex-wrap wrap, gap 0.25rem, margin-top 0.5rem

**8. Tag pill (.tag-pill)**:
- Display inline-block, padding 0.15rem 0.5rem
- Background: transparent, border 1px solid `var(--neon-cyan)`, border-radius 3px
- Color: `var(--neon-cyan)`, font-size 0.75rem, text-decoration none
- Hover: background `var(--neon-cyan)`, color `var(--bg-base)`, transition 0.1s
- IMPORTANT: This is an interactive element — neon color on the border/background is correct. Do NOT use neon color as body text.

**9. Document page**:
- `.doc-page`: max-width 720px, margin 0 auto
- `.doc-header`: margin-bottom 2rem, padding-bottom 1rem, border-bottom 1px solid `var(--bg-elevated)`
- `.doc-title`: font-size 2rem, font-weight 600, color `var(--text-primary)`, margin-bottom 0.5rem
- `.doc-date`: color `var(--neon-magenta)`, font-size 0.85rem, display block, margin-bottom 0.75rem
- `.doc-tags`: display flex, flex-wrap wrap, gap 0.35rem
- `.doc-content`: `h1` through `h3` use `var(--text-primary)` (NOT neon), line-height 1.8; `p` margin-bottom 1rem; `code` background `var(--bg-elevated)`, padding 0.1em 0.3em, border-radius 3px; `pre` background `var(--bg-elevated)`, padding 1rem, border-radius 6px, overflow-x auto; `a` color `var(--neon-cyan)`

**10. Tag pages**:
- `h1` for tag page: prepend `#` character, color `var(--neon-cyan)` (this is display, not body text — acceptable)
- `.tag-list`: list-style none, display flex, flex-wrap wrap, gap 0.5rem 1rem
- `.tag-count`: color `var(--text-secondary)`, font-size 0.85rem, margin-left 0.25rem

Write the complete CSS file. Do not leave placeholder comments — write the actual CSS rules.
  </action>
  <verify>
    <automated>cd /home/daniel/codes/loom && npm run build 2>&1 | tail -5 && node -e "
const fs = require('fs');
const css = fs.readFileSync('./public/styles/global.css', 'utf8');
const checks = [
  ['--bg-base', '#0D0D1A'],
  ['--neon-cyan', '#00E5FF'],
  ['--neon-magenta', '#FF2D78'],
  ['--neon-green', '#39FF14'],
  ['--text-primary', '#D0D0D0'],
  ['--font-mono', 'Fira Code'],
  ['fira-code', '@import'],
  ['.tag-pill', 'border'],
  ['.doc-grid', 'grid'],
  ['.category-heading', 'neon-green'],
];
let ok = 0;
for (const [label, contains] of checks) {
  if (!css.includes(label)) { console.error('MISSING:', label); continue; }
  ok++;
}
console.log('CSS checks:', ok + '/' + checks.length + ' passed');
if (ok < checks.length) process.exit(1);
" 2>&1</automated>
  </verify>
  <done>
`npm run build` succeeds. CSS check script confirms all color variables, font import, and key component classes are present. The site renders with dark background and neon accents (visual verification happens in Plan 05 checkpoint).
  </done>
</task>

</tasks>

<verification>
Run after build:
```bash
cd /home/daniel/codes/loom && npm run build 2>&1 | tail -5
```
Then visually open `dist/index.html` in a browser to confirm dark background and monospace font before the Plan 05 checkpoint.
</verification>

<success_criteria>
- `public/styles/global.css` exists with all CSS custom properties using exact color values from research (REQ-030, REQ-031)
- Fira Code font imported via `@fontsource/fira-code` (REQ-032)
- Body text uses `--text-primary` (#D0D0D0) — neon colors only on borders/backgrounds of interactive elements (REQ-034)
- `npm run build` succeeds with CSS in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-pipeline-and-site-foundation/01-04-SUMMARY.md`
</output>
